{
  "name": "nyc-jest-issue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "jest": "jest --coverage --env=node --runInBand",
    "test": "nyc jest --env=node --runInBand",
    "testAll": "nyc --all jest --env=node",
    "report": "nyc report --reporter=html --report-dir=nyc_coverage"
  },
  "dependencies": {
    "jest": "^22.0.5",
    "nyc": "^11.3.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
